# P13 Deferred Items Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve the 4 remaining P13 items: split the large test file, create conftest.py, fix FakeGitHub jq fidelity, add search predicate warning.

**Architecture:** The test file split (P13-020) and conftest.py (P13-013) are done together since the split naturally centralizes imports. FakeGitHub fixes (P13-006, P13-019) are independent.

**Tech Stack:** Python 3.10+, pytest, unittest, FakeGitHub

---

## Task 1: Create conftest.py and split test_gh_interactions.py (P13-013 + P13-020)

**Strategy:** Split 69 classes across 3,169 lines into 4 focused files by module domain. Create conftest.py with shared path setup to eliminate duplicate sys.path.insert calls.

**Split plan by module domain:**

| New file | Classes (by line range) | Module coverage |
|----------|----------------------|-----------------|
| `tests/conftest.py` | Shared path setup, fixtures | All paths centralized |
| `tests/test_gh_interactions.py` (keep, shrink) | Lines 60-445: commit.py, release_gate helpers, FakeGitHub infra | commit, release_gate helpers, FakeGitHub |
| `tests/test_sprint_runtime.py` (new) | Lines 446-2010: check_status, sync_tracking, update_burndown, bootstrap, populate, kanban | Sprint runtime modules |
| `tests/test_bugfix_regression.py` (new) | Lines 2544-3169: BH-xxx regression tests, integration tests, FakeGitHub strict mode | Cross-cutting regressions |

Files:
- Create: `tests/conftest.py`
- Create: `tests/test_sprint_runtime.py`
- Create: `tests/test_bugfix_regression.py`
- Modify: `tests/test_gh_interactions.py` (remove moved classes)
- Modify: All other test files (remove sys.path.insert, use conftest)

### Steps

- [ ] **Step 1: Create tests/conftest.py**
- [ ] **Step 2: Create tests/test_sprint_runtime.py** (extract runtime classes)
- [ ] **Step 3: Create tests/test_bugfix_regression.py** (extract regression classes)
- [ ] **Step 4: Trim test_gh_interactions.py** (remove extracted classes)
- [ ] **Step 5: Remove sys.path.insert from test files that conftest covers**
- [ ] **Step 6: Run full suite, verify 671 tests pass**
- [ ] **Step 7: Commit**

---

## Task 2: Fix FakeGitHub timeline pre-filtering (P13-006)

- [ ] **Step 1:** Add a test that verifies the jq expression from sync_tracking.py against sample timeline data
- [ ] **Step 2:** Run and commit

## Task 3: Add FakeGitHub search predicate warning (P13-019)

- [ ] **Step 1:** In FakeGitHub._extract_search_milestone, emit warning for unrecognized predicates
- [ ] **Step 2:** Run and commit
