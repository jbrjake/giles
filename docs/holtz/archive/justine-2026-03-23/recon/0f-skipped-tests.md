# 0f: Skipped Tests

No `@unittest.skip`, `@pytest.mark.skip`, or `xfail` markers found in test files.

All 1205 tests run unconditionally.

References to "skip" in test files are behavioral -- testing that production code correctly skips certain inputs (e.g., `test_missing_file_skipped`), not test framework skip markers.
