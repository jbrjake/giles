# 0d: Lint Results

No linter configured in CI (no ruff, mypy, or flake8 config files found in project root).

The project uses `from __future__ import annotations` consistently.
No type checker configured.
